import 'package:flutter/material.dart';

class PageSliver extends StatefulWidget {
  static const String path = '/sliver_page';
  const PageSliver({Key? key}) : super(key: key);

  @override
  State<PageSliver> createState() => _PageSliverState();
}

class _PageSliverState extends State<PageSliver> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
